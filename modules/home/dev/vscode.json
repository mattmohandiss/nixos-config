{
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,

  "nix.enableLanguageServer": true,
  "nix.serverPath": "nixd",
  "nix.serverSettings": {
    "nixd": {
      "eval": {
        "target": {
          "args": ["--flake", "/etc/nixos#surface"],
          "installable": ""
        }
      },
      "formatting": {
          "command": ["nixpkgs-fmt"]
      },
      "options": {
        "nixos": {
          "expr": "(builtins.getFlake (builtins.toString ./.)).nixosConfigurations.surface.options"
        },
        "home-manager": {
          "expr": "(builtins.getFlake (builtins.toString ./.)).nixosConfigurations.surface.options.home-manager.users.type.getSubOptions []"
        }
      },
      "nixpkgs": {
        "expr": "import (builtins.getFlake (builtins.toString ./.)).inputs.nixpkgs { }"
      },
      "diagnostic": {
        "suppress": ["sema-extra-with"]
      }
    }
  },

  "direnv.restart.automatic": true,
  "direnv.status.showOnStatusBar": true
}
